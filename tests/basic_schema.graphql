type User {
  lastName: String
  greeting: String!
  sayHello(name: String): String
  double(num: Int!): Int!
  allPrimes(nums: [Int]): Boolean!
  compare(a: String, b: Int): String
}

schema {
  query: User
}
